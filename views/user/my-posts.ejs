<%- include("../includes/head.ejs") %>
    <link rel="stylesheet" href="/css/general.css"> 
    <link rel="stylesheet" href="/css/home.css">
    <link rel="stylesheet" href="/css/post.css" /> 
    </head>

<body>
    <%- include("../includes/navigation.ejs") %>

    <main>

        <% if (posts.length > 0) { %>    
            <div class="cont">
                <div class="right-div">
                    <div class="post">
                        <% for (let post of posts) { %>
                            <%- include("../includes/post-template.ejs", {post: post}) %>
                        <% } %>
                    </div> 
                        
                    <!-- <a href="">testing testing</a> -->
                    <div style="border-bottom: 30px;">
                        <% if (!onePage)  { %> 
                            <% if (pageNo === 0) { %>
                                <a href="/my-posts?pageNo=<%= pageNo + 1 %>" class="button-7">Next</a>
                            <% } else if (lastPage) { %>
                                <a href="/my-posts?pageNo=<%= pageNo - 1 %>" class="button-7">Prev</a>
                            <% } else { %>
                                <a href="/my-posts?pageNo=<%= pageNo - 1 %>" class="button-7">Prev</a>
                                <a href="/my-posts?pageNo=<%= pageNo + 1 %>" class="button-7">Next</a>
                            <% } %> 
                        <% } %> 
                    </div>

                </div>
                <div class="left-div">
                    <div>
                        <h1 class="heading"><%= pageTitle %> </h1>
                    </div>
                    <div class="logo">
                        <img src="/assets/logo8.svg" alt="logo" />
                    </div>
                </div>
            </div>
        <% } else { %> 
            <h1>No Posts Found</h1>
        <% } %> 
    </main>


<%- include("../includes/end.ejs") %>
